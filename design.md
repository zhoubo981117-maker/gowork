# go work 移动端应用界面设计文档

## 设计原则

- **移动优先**：所有界面设计基于竖屏 (9:16) 和单手操作
- **遵循 iOS HIG**：应用风格完全对标第一方 iOS 应用
- **简洁高效**：最小化认知负荷，快速完成核心任务
- **深色模式支持**：完整的浅色/深色主题适配

---

## 屏幕清单

### 1. **主页 (Home Screen) - 岗位管理大厅**
- **核心内容**：展示所有已添加的岗位卡片列表
- **功能**：
  - 卡片式布局，每张卡片显示：公司名称、岗位名称、工作地点、面试状态标签
  - 顶部筛选栏：按"地点"和"面试状态"过滤
  - 底部浮动按钮：新增岗位
- **交互**：点击卡片进入岗位详情页

### 2. **设置页 (Settings Screen) - API 配置**
- **核心内容**：全局 AI 服务配置
- **功能**：
  - Base URL 输入框
  - API Key 输入框（密码隐藏）
  - 模型选择下拉菜单（预设 GPT-5.5、Claude Opus 4.8，支持自定义输入）
  - 保存/取消按钮
- **数据持久化**：所有配置保存到本地 SQLite

### 3. **岗位详情页 (Job Detail Screen)**
- **核心内容**：
  - AI 解析的核心字段展示（公司名称、岗位名称、工作地点、核心技能需求）
  - 人岗匹配度分析结果（文本段落）
  - 查看原始文档/图片的入口
- **功能**：
  - 状态切换下拉菜单/进度条：投递前 → 投递中 → 一面 → 二面 → 已拿 offer / 面试未通过
  - 标签页切换：信息展示 ↔ AI 辅导聊天室
- **交互**：状态改变实时保存

### 4. **AI 辅导聊天室 (AI Chat Screen)**
- **核心内容**：
  - 标准对话气泡界面
  - 顶部"切换 AI 身份"下拉菜单
  - 消息输入框 + 发送按钮
- **AI 身份列表**：
  1. **简历优化导师** - 针对 JD 挖掘简历亮点
  2. **严格的技术面试官** - 针对 JD 的技术栈进行高压提问
  3. **外企双语 HR** - 考察跨文化沟通、行为面试、英文交流
  4. **客观的复盘导师** - 协助用户对刚结束的面试进行总结
- **上下文注入**：System Prompt 自动注入当前岗位 JD、用户简历、面试状态
- **交互**：切换身份时清空聊天记录，重新开启对话

### 5. **简历/JD 导入页 (Import Screen)**
- **核心内容**：
  - 简历上传区（支持 PDF/Word/图片）
  - 岗位 JD 上传区（支持 PDF/Word/图片）
- **功能**：
  - 选择文件后显示预览
  - AI 智能解析按钮
  - 解析结果展示（提取的关键字段）
- **交互**：完成解析后自动保存到数据库，返回主页

### 6. **文档查看页 (Document Viewer)**
- **核心内容**：
  - 原始文档/图片全屏展示
  - 缩放、平移、旋转等基础操作
- **功能**：
  - 支持 PDF、图片、Word 文档预览
  - 返回按钮

---

## 主要用户流程

### 流程 1：初次使用 - 配置 API
1. 用户打开应用 → 进入主页（空状态）
2. 点击设置按钮 → 进入设置页
3. 输入 Base URL、API Key、选择模型
4. 保存配置 → 返回主页

### 流程 2：导入岗位并分析
1. 用户在主页点击"+"按钮 → 进入导入页
2. 上传简历和岗位 JD（支持多种格式）
3. 点击"AI 解析"按钮 → 后台调用 AI 接口
4. 解析完成 → 展示匹配度分析结果
5. 保存岗位 → 返回主页，卡片显示在列表中

### 流程 3：查看岗位详情并更新状态
1. 用户在主页点击岗位卡片 → 进入详情页
2. 查看 AI 解析的信息和匹配度分析
3. 点击状态下拉菜单 → 选择新状态（如"一面"）
4. 状态实时保存 → 返回主页，卡片标签更新

### 流程 4：与 AI 进行多轮面试辅导
1. 用户在详情页点击"AI 辅导聊天室"标签
2. 选择 AI 身份（如"严格的技术面试官"）
3. 开始多轮对话
4. 切换身份 → 清空聊天记录，重新开启新对话

---

## 色彩方案

| 用途 | 浅色 | 深色 |
|------|------|------|
| 主色调 (Primary) | `#0a7ea4` | `#0a7ea4` |
| 背景 (Background) | `#ffffff` | `#151718` |
| 表面 (Surface) | `#f5f5f5` | `#1e2022` |
| 文本 (Foreground) | `#11181C` | `#ECEDEE` |
| 辅助文本 (Muted) | `#687076` | `#9BA1A6` |
| 边框 (Border) | `#E5E7EB` | `#334155` |
| 成功 (Success) | `#22C55E` | `#4ADE80` |
| 警告 (Warning) | `#F59E0B` | `#FBBF24` |
| 错误 (Error) | `#EF4444` | `#F87171` |

---

## 状态枚举

| 状态 | 标签颜色 | 含义 |
|------|---------|------|
| 投递前 | 灰色 | 还未投递 |
| 投递中 | 蓝色 | 已投递，等待回应 |
| 一面 | 黄色 | 进入一轮面试 |
| 二面 | 橙色 | 进入二轮面试 |
| 已拿 offer | 绿色 | 成功获得 offer |
| 面试未通过 | 红色 | 面试被拒 |

---

## 数据模型概览

### Job (岗位)
- `id`: 唯一标识
- `companyName`: 公司名称
- `jobTitle`: 岗位名称
- `location`: 工作地点
- `status`: 面试状态 (enum)
- `jdContent`: JD 原始内容（文本或 Base64 图片）
- `jdFileUri`: JD 文件本地路径
- `resumeContent`: 简历原始内容
- `resumeFileUri`: 简历文件本地路径
- `coreSkills`: AI 提取的核心技能需求 (JSON 数组)
- `matchAnalysis`: 人岗匹配度分析文本
- `createdAt`: 创建时间
- `updatedAt`: 更新时间

### ChatMessage (聊天消息)
- `id`: 唯一标识
- `jobId`: 关联的岗位 ID
- `aiPersona`: AI 身份类型 (enum)
- `role`: 消息角色 (user / assistant)
- `content`: 消息内容
- `timestamp`: 时间戳

### APIConfig (API 配置)
- `baseUrl`: API 基础 URL
- `apiKey`: API 密钥
- `model`: 选择的模型名称
- `updatedAt`: 更新时间

---

## 交互细节

### 按钮反馈
- **主按钮**：按下时缩放到 0.97，配合轻微触觉反馈
- **列表项/卡片**：按下时透明度降至 0.7
- **图标/次要操作**：按下时透明度降至 0.6

### 加载状态
- 调用 AI 解析时显示加载动画
- 聊天消息发送时显示发送中状态

### 空状态
- 主页无岗位时：显示空状态插图 + "点击添加岗位"提示
- 聊天室首次打开：显示欢迎消息

### 错误处理
- API 配置缺失：提示用户前往设置页配置
- 文件上传失败：显示错误提示，允许重试
- AI 解析失败：显示错误信息和重试按钮

---

## 响应式设计

- **竖屏 (Portrait)**：所有内容垂直堆叠，卡片宽度 100% - 32px 边距
- **横屏 (Landscape)**：暂不支持（锁定竖屏）
- **安全区域**：所有内容避开刘海、底部指示器等系统区域

---

## 无障碍设计

- 所有交互元素最小点击区域 44×44pt
- 文本对比度符合 WCAG AA 标准
- 关键操作提供触觉反馈
- 支持系统字体大小缩放
